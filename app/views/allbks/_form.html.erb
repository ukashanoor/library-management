<%= form_for(allbk) do |f| %>
  <% if allbk.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(allbk.errors.count, "error") %> prohibited this allbk from being saved:</h2>

      <ul>
      <% allbk.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :isbn %>
    <%= f.text_field :isbn %>
  </div>

  <div class="field">
    <%= f.label :issue %>
    <%= f.check_box :issue %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
